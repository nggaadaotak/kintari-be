# Kintari Backend Environment Variables for Railway

# Gemini AI API Key (REQUIRED - Set in Railway Dashboard)
GEMINI_API_KEY=your_gemini_api_key_here

# Database (SQLite - auto-created, no need to change)
DATABASE_URL=sqlite:///./kintari.db

# CORS Origins (Update with your Railway frontend URL)
ALLOWED_ORIGINS=https://your-frontend.railway.app,http://localhost:3000

# Upload directory (relative path, auto-created)
UPLOAD_DIR=./uploads

# Railway auto-sets PORT - don't override
# PORT is managed by Railway
